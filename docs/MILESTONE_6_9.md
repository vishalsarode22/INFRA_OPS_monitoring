# Milestone 6.9 — Intelligence API Contract
Adds a read-only FastAPI contract over existing persisted snapshots. Apply with `python scripts\\apply_milestone_6_9.py`.
Endpoints: health, metrics, incidents, intelligence, overview, and incident RCA. No credentials are returned and the UI reads snapshot data rather than runtime internals.
